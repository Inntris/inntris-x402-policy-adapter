# `@inntris/decision-core`

Rail-independent schemas, RFC 8785 canonicalisation, SHA 256 hashes, Ed25519 signing, decision
fingerprints, replay-store contracts and stable reason codes.

This package has no dependency on x402.
